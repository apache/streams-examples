twitter {
  endpoint = sample
  track = [
    "data"
  ]
}
elasticsearch {
  hosts += ${es.tcp.host}
  port = ${es.tcp.port}
  clusterName = elasticsearch
  index = twitter_userstream_elasticsearch_it
  type = activity
  forceUseConfig = true
}